# 1. checkout to feature branch
git checkout -b associations
# Explain pre work files then go to index.js

# 5. run seed script
npm run seed
# Go to main.js

# 11. git workflow
git add .
git commit -m "some message"
git push --set-upstream origin associations